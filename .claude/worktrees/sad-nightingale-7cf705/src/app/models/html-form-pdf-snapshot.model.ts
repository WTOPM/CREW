/** sessionStorage key — parent window writes, HTML form iframe reads once. */
export const HTML_FORM_PDF_SNAPSHOT_STORAGE_KEY = 'crew-html-form-pdf-snapshot';

/** Query flag: load PDF snapshot from sessionStorage instead of ?data=. */
export const HTML_FORM_PDF_DATA_PARAM = 'pdfData';
