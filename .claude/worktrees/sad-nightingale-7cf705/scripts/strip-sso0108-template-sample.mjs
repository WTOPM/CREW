/**
 * No-op: SSO-0108 template (123.pdf) ships clean — no sample text to strip.
 * Kept for backwards compatibility with older build scripts.
 */
console.log('SSO-0108 template is already empty — nothing to strip.');
